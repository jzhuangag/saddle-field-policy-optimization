# Archived HPC4 PPO-RARL provenance

This directory is a local read-only copy of selected source files and the experiment protocol from:

`/project/vincentlau/jzhuangag/rarl_vic_full_1m_20260719`

The copy was retrieved on 2026-09-03 from host `login2` through the configured `hpc4` SSH alias.
The source repository reported commit `ade90a755e9fc0ec18a2399372cb013fd61dd117` with modified and untracked experiment files.
Consequently, the commit hash alone does not identify the copied state; the files in this directory are retained as provenance and must not be treated as a clean upstream checkout.

The archived experiment reports five-seed results for Hopper-v4, Walker2d-v4, and HalfCheetah-v4.
They are diagnostic evidence only and are not the frozen ICLR evaluation protocol.
New ICLR code will be developed outside this provenance directory and will write new results to timestamped locations.
